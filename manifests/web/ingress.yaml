apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "app-api"
    kubernetes.io/ingress.class: "gce"
  name: app-api
spec:
  defaultBackend:
    service:
      name: app-api
      port:
        number: 80
  rules:
    - http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: app-api
                port:
                  number: 80